# QA status count check

Expected after issue #12:

```text
total       30
keep        22
provisional  0
remake       8
```

These values match `assets/source/creatures/catalog.json`. This stream intentionally leaves `binaryImportStatus` pending because #11 owns the missing original source-bundle import.

# #12 regeneration notes

Generation date: 2026-09-09
Style target: `poseidon-sunlit-square-v1`

The batch deliberately targeted the seven remake species, the six provisional species, and `splendid-toadfish`. Dense-gallery review kept the established bright aqua/sunlit square vocabulary, but biological identity was the acceptance gate.

Rejected attempts are intentionally not treated as source revisions merely because they look polished. Recurrent failure modes were:

- Atlantic species drifting to familiar Indo-Pacific lookalikes (`blue-tang`);
- reversed or generic spot/bar patterns (`spotted-trunkfish`, `porkfish`);
- generic wrasse/toadfish morphology (`spanish-hogfish`, `splendid-toadfish`);
- shell-only presentation when visible animal anatomy is useful (`queen-conch`);
- generic octopus identity (`caribbean-reef-octopus`).

This record explains why #12 can close with some entries still `remake`: its acceptance contract explicitly permits stronger replacements **or explicitly blocked entries**. The next art pass should start from these diagnostic failures rather than regenerating generic “Caribbean” animals.
